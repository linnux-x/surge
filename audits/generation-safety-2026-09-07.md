# 生成与模块校验复审 · 2026-09-07

本轮在本地修复分支继续检查，远端 main 已重新 fetch，仍为
`74c06205706c106d8bcc63530e1a95d253b958b0`。现有作者、公开 Raw 路径和生产规则保持原状。
以下问题均已修复；修复不代表已推送、合并或在真实设备运行。

## 发现与修复

| 优先级 | 问题与触发条件 | 修复 |
|---|---|---|
| P1 | CIDR 裁剪忽略选项；带 no-resolve 的大网段会删除不带该选项的小网段，改变未解析域名的匹配行为 | 仅在选项集合完全相同时裁剪，覆盖 IPv4 / IPv6 回归用例 |
| P1 | HTTP 成功但上游为空或只有注释时，其他来源可能掩盖缺失，流程继续生成 | 每个来源清洗后必须非空，空源直接阻断，不静默改用其他源 |
| P2 | 规则校验前已覆盖目标文件；无效上游导致失败时留下坏文件 | 在同目录临时文件处理和校验，通过后原子替换单个规则文件，失败清理临时文件并保留旧文件 |
| P2 | DNS 模块白名单只检查行首 `[`，而且仅要求存在一个合法 URL；CI 未复用模块约束 | 统一 Python 校验器，检查去除空白后的所有配置节、每条远程规则引用，CI 与同步流程共用 |
| P2 | README、贡献指南和边界文档仍称每日同步由 Hermes 调度 | 核实本机 Codex 三个任务均 ACTIVE 后更新当前文档；历史审计记录不改写 |

`no-resolve` 影响 DNS 解析与规则匹配，不是可以忽略的装饰选项。
依据：[Surge 官方规则说明](https://manual.nssurge.com/rules/overview.html)、
[规则集的行内选项](https://manual.nssurge.com/rules/ruleset.html)。

## 验证范围

- 修复前测试复现 CIDR 选项丢失（IPv4 和 IPv6）、空源未阻断、校验失败覆盖旧文件；修复后全部通过。
- 36 项单元测试、130 项路由测试、完整仓库校验、Clash 校验、确定性发布收据比对通过。
- 使用上一轮 46 URL 冻结缓存，分别以修复前后生成器完整生成全部 26 个规则集并裁剪 Global；两次生成通过，有效规则集合差异为零。证明本次采样没有引入额外规则，不证明未来上游无异常。
- DNS 模块测试覆盖当前模块、缩进的 Script / MITM / URL Rewrite、重复 Host 节、不受信任 URL、目录穿越和查询参数。

本轮原子替换是单文件保证，不是整个 26 文件流水线的事务回滚；后续步骤失败时仍须按现有
发布门禁阻断提交。未触发 GitHub Actions、自动任务或设备网络切换；线上 CI 和客户端行为尚未验证。

## 联网复核

2026-09-07 22:28（北京时间）执行 `scripts/audit_rules.py`：0 ERROR、0 WARN、6 INFO。
六条信息项为服务专属的共享平台子域。Kelee 两个主入口仍返回 HTTP 403，审计明确使用
已登记 fallback；0 WARN 不代表全部上游实时可用，此限制继续保留，不删除该作者。
日志保留在本机 `.automation-state/surge-repo-audit-20260907/online-audit.log`。

## 发布前复核 · 2026-09-08

已整合最新主线 `6accab4`，保留当日定时规则更新。补齐 CIDR 校验器的选项比较，
增量参数必须是已知规则集名称组成的 JSON 数组，格式错误直接阻断；Global 裁剪也改为
先校验再写入。39 项单元测试及 130 项路由测试通过，仓库、Clash、收据检查通过。
再次离线回放确认本轮三个分发入口切换的合并规则集合差异仍为零；另两个入口继续保留原来源。
